# 新闻推荐智能体 - 功能说明文档

## 📋 功能概述

新闻推荐智能体是一个基于自然语言理解的智能推荐系统，用户可以通过输入自然语言描述自己的阅读需求，系统会自动解析意图并生成个性化推荐。

## ✨ 核心特性

### 1. 自然语言理解
用户可以使用日常语言描述需求，例如：
- "给我推荐今天的科技新闻"
- "我想看和人工智能有关的国内新闻"
- "不要娱乐新闻，多推荐财经和国际新闻"
- "根据我最近看的内容推荐一些深度文章"
- "换一批，但不要和刚才太相似"

### 2. 智能意图识别
系统能够自动识别：
- **关键词提取**：从用户输入中提取核心关注点
- **类别识别**：识别用户感兴趣的新闻类别（科技、财经、国内、国际、娱乐、体育、军事、汽车）
- **时间范围**：识别时间约束（今天、最近两天、近一周等）
- **排除条件**：识别用户不想看的内容
- **特殊查询**：识别刷新、基于历史、相似内容等需求

### 3. 多维度综合评分算法

采用加权综合评分公式：
FinalScore = α * SimilarityScore (相似度 30%) + β * HeatScore (热度 20%) + γ * FreshnessScore (新鲜度 25%) + δ * UserInterestScore (用户兴趣 15%) + ε * QualityScore (质量 10%) - λ * RepetitionPenalty (重复惩罚 15%)
#### 评分维度详解：

1. **相似度分数 (SimilarityScore)**
   - 计算新闻关键词与用户查询的Jaccard相似度
   - 计算新闻内容与用户标签的匹配度
   - 综合权重：查询关键词60% + 用户标签40%

2. **热度分数 (HeatScore)**
   - 基于新闻阅读量和评论数
   - 归一化处理：`0.4 * (阅读量/10000) + 0.5 * (评论数/1000)`

3. **新鲜度分数 (FreshnessScore)**
   - 使用指数衰减模型：`exp(-k * Δt)`
   - k=0.1为时间衰减系数
   - Δt为新闻发布时间距当前的天数

4. **用户兴趣分数 (UserInterestScore)**
   - 基于用户历史选择的标签
   - 考虑标签权重（用户主动选择的标签权重更高）
   - 计算新闻关键词与用户标签的匹配度

5. **内容质量分数 (QualityScore)**
   - 内容长度评分（理想长度500-2000字）
   - 多媒体加分（有图片+0.1，有视频+0.1）

6. **重复惩罚 (RepetitionPenalty)**
   - 已浏览新闻惩罚：0.8
   - 与近期浏览内容相似度过高惩罚：相似度 * 0.5

### 4. 推荐理由生成

系统会为每篇推荐新闻生成可解释的推荐理由，包括：
- 与用户历史行为的关联
- 关键词匹配情况
- 热度水平
- 新鲜度
- 类别匹配
- 内容质量

示例：
推荐理由：与你关注的「人工智能、芯片」话题高度相关；包含你搜索的关键词「AI」；当前热门文章（阅读5000+）
## 🚀 使用方法

### 前端访问

1. 登录系统后，点击导航栏的 **"智能推荐"** 按钮
2. 在对话框中输入你的需求，或使用快捷建议按钮
3. 查看推荐结果和推荐理由
4. 可以按综合评分、最新发布、最热等方式排序

### API调用

**接口地址**: `GET /api/intelligent/recommend/`

**请求参数**:
- `user_input`: 用户输入的自然语言（必填）
- `userid`: 用户ID（必填）
- `top_n`: 返回数量（可选，默认20）

**响应示例**:
json { "status": "200", "message": "Success", "data": { "user_input": "给我推荐今天的科技新闻", "total": 20, "recommendations": [ { "newsid": 12345, "title": "AI芯片技术取得重大突破", "date": "2024-01-15 10:30", "pic_url": "["http://example.com/image.jpg"]", "mainpage": "文章内容摘要...", "origin": "科技日报", "category": 1, "readnum": 5000, "comments": 120, "keywords": "AI,芯片,技术突破", "hot_value": 0.85, "recommend_score": 0.87, "score_breakdown": { "similarity": 0.92, "heat": 0.85, "freshness": 0.95, "user_interest": 0.88, "quality": 0.82, "repetition_penalty": 0.0 }, "reason": "与你关注的「AI、芯片」话题高度相关；包含你搜索的关键词「科技」；最新发布" } // ... 更多推荐 ] } }
## 🔧 技术实现

### 后端架构

1. **NewsRecommendAgent类** (`Recommend/NewsRecommendAgent.py`)
   - 核心推荐引擎
   - 意图解析
   - 评分计算
   - 理由生成

2. **Django视图** (`newsServer/models/intelligent_agent.py`)
   - API接口封装
   - 数据格式化

### 前端架构

1. **IntelligentRecommend.vue** (`NewsPage/src/views/IntelligentRecommend.vue`)
   - 聊天式交互界面
   - 推荐结果展示
   - 评分可视化

2. **API接口** (`NewsPage/src/api/index.js`)
   - intelligentRecommend函数

## 📊 配置调优

### 调整评分权重

在 `NewsRecommendAgent.__init__()` 中修改：
python self.weights = { 'similarity': 0.30, # 相似度权重 'heat': 0.20, # 热度权重 'freshness': 0.25, # 新鲜度权重 'user_interest': 0.15, # 用户兴趣权重 'quality': 0.10, # 质量权重 'repetition_penalty': 0.15 # 重复惩罚系数 }
### 调整时间衰减
修改新鲜度衰减系数k：
python self.freshness_decay_k = 0.1 # 值越大，旧新闻降权越快
## 🎯 应用场景

1. **个性化新闻检索**：用户可以精确描述想看的内容
2. **探索性阅读**：发现感兴趣但未主动搜索的内容
3. **深度学习**：系统通过用户行为不断优化推荐
4. **内容分发**：提高用户阅读满意度和停留时间

## 📝 注意事项

1. 确保数据库中存在以下表：
   - `news_api_user` (用户信息)
   - `news_api_newsdetail` (新闻详情)
   - `news_api_newshot` (新闻热度)
   - `news_api_newssimilar` (新闻相似度)
   - `news_api_history` (浏览历史)
   - `news_api_recommend` (推荐记录)

2. 需要安装jieba分词库：
   

3. 首次使用时，用户标签可能为空，系统会给予默认中等分数

4. 推荐效果会随着用户使用时间增长而优化

## 🐛 故障排查

### 问题1：推荐结果为空
- 检查数据库中是否有符合条件的新闻
- 检查用户ID是否正确
- 查看日志文件 `Recommend/intelligent_agent.log`

### 问题2：评分异常
- 检查各维度数据是否完整
- 确认日期格式是否正确
- 验证用户标签是否存在

### 问题3：响应速度慢
- 考虑添加缓存机制
- 优化数据库查询（添加索引）
- 减少返回数量top_n

## 📈 未来优化方向

1. 引入BERT等深度学习模型进行语义理解
2. 实现协同过滤推荐
3. 添加实时反馈机制（用户对推荐结果的点赞/点踩）
4. 实现A/B测试框架
5. 添加热门推荐和 trending 话题
6. 实现跨用户迁移学习


